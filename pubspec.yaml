name: discord_logger
description: A simple flutter package to send message to discord channel via discord bot
version: 1.0.1
homepage: https://github.com/slimpotatoboy/discord_logger

environment:
  sdk: '>=2.19.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
